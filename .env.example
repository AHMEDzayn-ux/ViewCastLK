YOUTUBE_API_KEY=
# Supabase Transaction-mode pooler connection string (port 6543), not the
# direct connection (port 5432) - find it under Project Settings -> Database
# -> Connection string in the Supabase dashboard.
SUPABASE_DB_URL=
SUPABASE_BACKUP_DB_URL=
